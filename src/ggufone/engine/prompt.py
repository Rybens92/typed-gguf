"""Prompt assembly + candidate (token) mapping (SPEC 2.3 steps 1 and 4)

Milestone: E1b.

The shared prefix is byte-identical for every question of a request;
candidate token sequences must be pairwise distinct.
"""
