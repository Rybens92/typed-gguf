"""Readout math: restricted softmax, confidence, score, coverage (SPEC 2.4)

Milestone: E1b.

These three names are pinned by docs/verify_runtime_contract.py section D
and must match the oracle mirror exactly:
    restricted_softmax, confidence_normalized_peak, score_weighted_mean
"""
